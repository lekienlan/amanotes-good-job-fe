## Amanotes Good Job Frontend

### Prerequisites
- Node.js and Yarn installed

### Install dependencies
```bash
yarn install
```

### Start the dev server
```bash
yarn dev
```

The app will start on the port configured by Vite (usually `http://localhost:5173`).

### Build for production (optional)
```bash
yarn build
```
